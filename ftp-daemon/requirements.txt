# Python 3.8+ 에서는 보안 수정이 반영된 2.32 이상을 쓰고,
# CentOS 7(Python 3.6/3.7) 환경에서는 설치 가능한 최신 버전으로 떨어뜨린다.
requests>=2.32.0; python_version >= "3.8"
requests>=2.27.1; python_version < "3.8"
urllib3<2          # v2+ requires OpenSSL 1.1.1+; CentOS 7 has 1.0.2k
psutil>=5.9.0
