# Changelog

커밋 히스토리를 날짜별(최신 순)로 정리한 기계적 기록. 사람이 읽는 요약은 [README.md](README.md) 참고.

## 2026-09-03
- 로그 조회 목록에서도 CWD 존재 확인 건 제외 (`3069136`)
- 스키마 적용이 서비스를 굶기지 않게 — lock_timeout + 소유권 이관 최소화 + 인덱스 CONCURRENTLY (`0857f7b`)
- 배포 순서 수정 — 새 코드 복사를 재시작 직전으로 (`f38a6ee`)
- CWD 존재 확인은 실패로 세지 않는다 — 폴더 이동 실패만 집계 (`f5c3b70`)
- CWD 실패 원인 분석 화면 추가 + 제외 경로 기준 일원화 (`87032ad`)
- CWD 실패 알림에 원인 단서 추가 — 경로 집중도 표시 + 제외 경로 설정 (`bc6502a`)
- 그룹 관리에서 장비 등록 — 승인된 장비를 그룹에 배정 (`7c76819`)
- .env 의 앱 미정의 키 제거 — DB_PASSWORD 로 인한 기동 실패 차단 (`5be3752`)
- nginx 1.30 설치 (nginx.org 저장소) + 버전별 http2 렌더링 (`dfc7606`)
- 설치 스크립트 실패 두 건 수정 — init.sql 권한, .env 조용한 종료 (`ef943a8`)
- WAS 설치 시 Let's Encrypt 인증서 발급 + 자동 갱신 (`9086a7b`)
- 데몬 설치 시 WAS 도메인 입력 — 프롬프트 또는 SOLTRACE_WAS_URL (`89eb487`)
- API 가이드 페이지 추가 — 상단 탭에서 조회 API 사용법 확인 (`063220d`)
- changelog 갱신 (`2f7bee4`)
- 계정별 API 키 발급 — 조회 전용 토큰으로 데이터 연동 (`f3caef6`)
- changelog 갱신 (`4f7124c`)
- 보안 점검 반영: IP 허용목록 우회 차단, OpenAPI 비공개, nginx 강화 (`1ed45e0`)
- changelog 갱신 (`a66745b`)
- 전송 속도 알림: 작은 파일 대량 전송 오탐 제거 + 임계값 설정 페이지 이동 (`1c976e1`)
- changelog 갱신 (`6ba9f36`)
- 샘플(데모) 페이지 추가 — 백엔드 없이 화면 확인 (`6032e78`)
- changelog.md 추가 — 커밋 히스토리 날짜별 정리 (`f61d877`)
- 대량 데이터 보관 개선 + 죽은 코드/중복 정리 (`11433b8`)

## 2026-06-30
- 고객사 계정 + groups.customer 기준 데이터 격리 추가 (`7f649da`)

## 2026-06-18
- fmtTime: timezone 없는 ISO 문자열을 UTC로 강제 파싱 (`5f647d0`)
- 코드리뷰 수정: HMS 시간 KST, fmtMbps 이름 명확화, dev_filter 변수명 통일 (`56fadb1`)
- 알람 버그 수정: 시간 이중변환, 단위 오표시, 다중워커 이중발송 (`1197edb`)
- CWD / 노이즈 제외를 WAS ingest로 이동 (`b23ded4`)
- 데몬: CWD / 실패 무시 — chroot 환경 클라이언트 초기화 노이즈 (`d6c6828`)
- 서비스 건강도: 기간 내 활동 없는 장비도 전체 표시 (`9ba5a93`)
- README 전면 업데이트: 구조·데몬 동작·이번 세션 변경사항 반영 (`08199b3`)
- 서비스 건강도 fail_totals: transfer_fails를 upload/download만으로 한정 (`834c63b`)
- 대시보드 전송 실패 드릴다운: upload/download만 필터링 (`b3a8fe3`)
- 장비 목록: 하트비트 끊긴 stopping 상태 → 미확인으로 표시 (`e511bb4`)
- 세 가지 버그/gap 수정 (`3166445`)
- 데몬: 비활성/삭제 장비 구분 처리 (`bec6094`)
- 데몬: Python 3.8 미만 호환 — unlink(missing_ok=True) 제거 (`5f3cba3`)
- 데몬: WAS 점검 중 연결 실패 시 종료하지 않고 버퍼 저장 후 재시도 (`d83a995`)
- bulk importer 진행 상황 로그 강화 (`52a065f`)
- 로그 조회 기본 범위 안내 문구 추가 (`7a1c7eb`)
- _ensure_partitions: ftp_logs_default에 데이터 있는 월은 파티션 생성 건너뜀 (`d2a005a`)
- ftp_logs_default 재배치 SQL 스크립트 추가 (`df408cf`)
- 로그 조회 범위 확대 및 과거 월 파티션 자동 생성 (`b91c295`)
- bulk importer: glob 패턴 지원 및 --no-transfer 플래그 추가 (`7f753b6`)
- 보안 강화: XSS 차단, 보안 헤더 추가, SMTP 제거 (`ff8b829`)
- 범례 테이블 컬럼 정렬 기능 추가 (`5ed13aa`)
- 범례 표 형태로 변경: 사용자/그룹명  (`bcffd89`)
- 대시보드 범례에 최대값·현재값 추가 (사용자별/그룹별 시간대 차트) (`f9ee849`)
- 버그 수정: 대시보드 실패 건수를 ftp_logs 직접 집계로 변경 (롤업 지연 불일치 해소) (`a6737b0`)
- 버그 수정: 드릴다운 24H 시간 손실, 대시보드 재진입 시 날짜 범위 초기화 (`e628aae`)
- 사용자별 사용량: bytes → 전송 건수(업로드+다운로드 카운트)로 변경 (`571cadd`)
- 대시보드: 조회 기간 내 사용량 0인 사용자/그룹 차트에서 제외 (`83d9222`)
- 대시보드 시간대별 차트 드래그 줌 추가 (chartjs-plugin-zoom) (`59aa6d0`)
- 버그 수정: 시간대별 사용량 bytes 합계 int → bigint (2GB 초과 시 overflow 500 오류) (`cc042fa`)
- 대시보드 UX 개선: 기본 조회 24H, 범례 영역 확장, 카드 컬러 테두리 (`4036566`)
- 차트 선 곡선 → 직선으로 변경 (tension 0.3 → 0) (`fed7674`)
- 사용자별/그룹별 시간대 차트 한 행으로 배치 (col-lg-6 각각) (`7fe231d`)
- UTC 표기 제거/수정: 차트 제목 (UTC) 삭제, 알림 시각 UTC→KST (`111c0d9`)
- 시간대별 사용량 카드 제목 변경: 장비 그룹별 시간대별 사용량 (`6c917c3`)
- 사용자별 사용량: 도넛 → 시간대별 선 차트로 변경 (`9b92979`)
- 시간대별 범례: 텔코 제거(한 줄), 클릭 동작 → 해당 그룹만 표시 (`81a06e5`)
- 대시보드 UX 수정: 사용자 범례 2줄 레이아웃, 시간대 x축 틱 간격 개선 (`8abb662`)
- 시간대별 사용량: 그룹별 선 차트 + 우측 토글 범례, 사용자 범례 정렬 수정 (`c0d2255`)
- 시간대별 사용량: hour-of-day 집계 → 기간 시계열로 변경 (`1918204`)
- 시간대별 사용량 그룹 필터 추가 (`b5db2e9`)
- 대시보드 UX 개선: 사용자별 사용량 범례 레이아웃 + 시간대별 사용량 차트 (`b90d35b`)
- 대시보드 날짜 범위 timezone 버그 수정 + 기간 표시 추가 (`b5b006e`)

## 2026-06-17
- 로그 조회 검색 초기화 버튼 추가 (`2f845a5`)
- 현재 접속 IP: 리버스 프록시 헤더 우선 참조 (`61a3964`)
- 마이그레이션 순서 수정: 중복 제거 → 백필 → 인덱스 생성 (`772eed5`)
- 중복 방지: row_hash 기반 ftp_logs dedup 구현 (`57bbb3c`)
- 코드 리뷰 수정: 타이머 클래스 리셋, CSS 정리, 드릴다운 타이밍 수정 (`a176acb`)
- 로그 조회 상태 필터 추가 및 CWD 실패 드릴다운 (`aec5a4d`)
- DB 인덱스 최적화: 누락된 5개 인덱스 추가 (`a7b27ca`)
- 유지보수성 개선: CSS 분리, 클래스 기반 레이아웃, 코드 정리 (`26bf9c1`)
- JS 캐시 버스팅 자동화: git 커밋 해시로 버전 주입 (`2355f09`)
- 세션 타이머를 topbar 우측 상시 표시로 변경 (`14afe6a`)
- 세션 만료 임박 시 재로그인 대신 세션 연장 기능 추가 (`fa74d3a`)
- 그룹 관리 테이블 서비스 컬럼 위치 조정: 업로드 도메인 뒤로 이동 (`7da5bbf`)
- 상단 탭바 sticky 고정 및 설정 메뉴 중앙 이동 (`4e95af7`)
- 로그 조회 버튼 무응답 수정: 에러 처리 및 캐시 버스팅 (`5ab2293`)
- 로그 조회에 파일명/경로 부분 일치 검색 필터 추가 (`5b2f1d2`)
- 그룹 관리 테이블에 서비스(application) 컬럼 추가 (`af8efa7`)
- Fix log table header wrapping on narrow viewport (`e0c144e`)
- Block Chrome autofill on log filters using readonly trick (`90730ba`)
- Fix log table collapsing on narrow viewport (`b3e3bb6`)
- Clear log filter fields on page init to override browser autofill (`1f022c6`)
- Fix browser autofill injecting login credentials into log filters (`aea12a6`)
- Log filter: add telco, reorder fields, group service tooltip (`9126db5`)
- Fix doughnut start angle; expand user chart to 10 items (`c1ad7b5`)
- Dashboard: sort user chart clockwise by usage; custom right legend (`7384ee5`)
- Simplify IP restriction to web login only; remove username change (`9e5316d`)
- Add ID/PW login, IP access restriction, and session expiry (`61f7d32`)
- Rearrange dashboard layout: 3-row grid (`03b6013`)
- Unify log filter behavior: selects auto-search, inputs on Enter (`9fafeef`)
- Fix group tooltip: show customer field instead of telco (`0df6c72`)
- Add uninstall instructions to README (`01a8a71`)
- Show telco on group usage chart tooltip (`a814235`)
- Dashboard: add day/week/month quick buttons and date range search (`f519cc4`)
- Add client_ip GIN trigram index for ILIKE search performance (`13836e6`)
- Hide login/logout by default in log search UI (`9cc0434`)
- Add skip_login_logout config option (`8249aed`)
- Remove ProtectSystem: /usr bind mount hides /usr/service submount (`0933175`)
- Fix ACL: setfacl -R overwrote rx with r on log directory itself (`d440f75`)
- Switch ProtectSystem=strict to full to fix /usr/service access (`a700ec9`)
- Verify ACL access after setfacl in install.sh (`8697379`)
- Restore hardening for Rocky 8; fix ReadOnlyPaths scope (`f47e997`)
- Fix service file CentOS 7 / systemd 219 compatibility (`05d41a5`)
- Apply code review fixes (`aa081d2`)
- daemon: fix ACL — add x on parent dirs, rx on log dir (`dd7fd99`)
- daemon: add ReadOnlyPaths for proftpd log dir (ProtectSystem=strict fix) (`8f684a3`)
- daemon: security hardening (`48b8760`)
- Group: auth → application field; device table header center-align (`d447421`)
- Settings/devices: center-align header toolbar, revert nav layout change (`40ab1b5`)
- Settings/devices: center-align table columns (`86e2c5d`)
- daemon: config.ini.example was_url http → https (`3f2955c`)
- Log table: uniform 140px columns (except file path), localStorage v3 (`51b6f4c`)
- Log table: show device IP in 업로드 장비 column, hostname in tooltip (`3449128`)
- Log filter: fix two-line wrapping, tighten spacing (`98bca4a`)
- Navbar: single-line menu items, clearer bottom border (`8193268`)
- Log table: widen device/action cols, left-align file path, versioned col widths (`0ed156e`)
- Fix col resize total width, nav wrap, telco icon (`f80517b`)
- Fix #logPage selector bug — correct to #page-logs (`60ffd2b`)
- Fix col resize: border-right separator + getBoundingClientRect boundary detection (`5f7c878`)
- Log table: fix col resize, full date format, header label updates (`655cc4f`)
- Fix all code review issues (`c0b6d1a`)
- Fix log col resize (use getBoundingClientRect); center-align all columns (`f01a341`)
- install.sh: auto-start daemon after install (`f819a5c`)
- README: add CentOS 7 daemon install guide and link to ftp-daemon/README.md (`eec601b`)
- Log table: center-align columns, single-row filter, rename inline, resize handles (`e789331`)
- Log device tooltip: show daemon host IP instead of client IP (`6ccf5dd`)
- Center nav menu; expand log filter to two rows (`d696311`)
- Dashboard/log UX improvements from screenshot review (`28635eb`)
- Log table: resizable columns, device tooltip with IP; add ftp-daemon README (`098b219`)
- Log table: fixed column widths, compact time format, truncate device/user (`189cc69`)
- Fix file path display: strip proftpd quotes; escape title attr; wrap rename path (`fb55d9c`)
- Add daemon self-update via web UI (`528b78d`)
- Fix log timestamp timezone: treat proftpd local time as system tz, convert to UTC (`5e20582`)
- install.sh: include soltrace_bulk.py in download list (`107a3b1`)
- Pin urllib3<2 for CentOS 7 OpenSSL 1.0.2k compatibility (`858a1fa`)
- Add daemon install.sh; relax requirements for Python 3.6+ compat (`a1cbcac`)
- Fix code review issues: notifier refactor, nginx cache, loginModal safety (`21f1085`)
- Split inline JS into feature files; add nginx long-cache for /static/js/ (`71a14f3`)
- Add favicon, group auth field, device OS/kernel info (`ca741c8`)

## 2026-06-16
- Compact Slack alert format: single section, one line per alert (`f283f23`)
- Improve alert format: KST time, [telco]group label, rich Slack blocks (`dcbb2a9`)
- Fix HMS: add X-reqsite header; remove stale .env hint text (`fec9139`)
- Support Slack webhook format; auto-detect hooks.slack.com URL (`efe1f7c`)
- Derive HMS telco_name from device group instead of config (`bac0674`)
- Simplify HMS config; add notify mute toggle (`eac4111`)
- Add per-channel test buttons for webhook, HMS, and email notifications (`8afbacb`)
- Add HMS mail gateway channel; group management pagination and telco spacing (`c7c2d96`)
- Fix update popup: race condition, stale message, caching (`b6a5401`)
- Fix code review issues: dispatch bool, index, pw mask, batch query (`f66803c`)
- Add webhook/SMTP notification settings UI (`50967bf`)
- Add ssl_verify option to daemon for HTTPS support (`9c00f46`)
- Add CWD 550 tracking, IP column/filter in log view (`ce07b97`)
- Fix group table column alignment: single table with section header rows (`c796efa`)
- Fix extra closing brace causing JS parse error (`84620c3`)
- Add telco filter buttons to group management (`9332647`)
- Group management: organize by telco with per-section table (`cd550f7`)
- Group management: card grid → wide table layout (`cff14bb`)
- Make customer/upload_domains fields larger; modal → modal-lg (`56f1653`)
- Enhance menu icons; fix settings layout scrollbar (`6d59aa8`)
- Add customer and upload_domains fields to groups; fix settings tab wrap (`651ddca`)
- Fix group card edit button: layout wrap and onclick JS error (`74b925e`)
- Add one-liner daemon install script (no git clone needed) (`58b8a7c`)
- Support .gz log files in bulk importer (`bc9fc7d`)
- Fix red bar after web update; deepen Sol orange color (`314d68d`)
- Brand SolTrace: Sol orange, Trace black, click → dashboard (`3d9dcc4`)
- Add failure count donut for transfer/login fails on service health page (`ec82dcc`)
- Compact dashboard into one row with donuts and hover emphasis (`a89e7db`)
- Surface git-fetch failures in update check; fix repo ownership (`ce2dc58`)
- Log search by device group; fix dashboard chart sizing (`f96ba1b`)
- Drop group_type; groups are services under a telco (`3fc840e`)
- Split settings into tabs; telco list; link telco to groups (`e34d24a`)

## 2026-06-15
- Fix git dubious-ownership in self-update/update as root (`c6da86f`)
- Switch to light top-nav layout; settings as right-side gear with left sub-tabs (`a915114`)
- Install self-update wrapper + sudoers from update_rocky8.sh too (`3f3525b`)
- Restructure dashboard and move device/group mgmt under settings (`f9dae82`)
- Transfer table ownership to soltrace after init.sql on update (`888d866`)
- Add settings page: password change, telco info, git self-update (`368c19d`)
- Use listen http2 syntax for older nginx compatibility (`2cc9d87`)
- Add HTTPS (Let's Encrypt) for soltrace.mbone.net (`2694fee`)
- Move app object ownership to soltrace without REASSIGN OWNED (`d4adc30`)
- Transfer DB ownership to soltrace for runtime partition DDL (`8ec8e58`)
- Install postgresql16-contrib for pg_trgm extension (`0f81d81`)
- Fix silent installer abort in _rand password generator (`ed3a90e`)
- Enable PowerTools/CRB repo in Rocky 8 installer (`7979fe5`)
- Add scheduled incremental DB backup with 3-year retention (`6fe4d21`)
- Add FTP service-impact anomaly detection (`2667c49`)
- Fix critical dashboard bug and partition/cascade edge cases (`e6f8f98`)
- Fix bulk insert incompatibility with GENERATED ALWAYS AS IDENTITY (`1962404`)
- Add Excel export and improve log page UI (`b2cebf1`)
- Add monthly range partitioning for ftp_logs (`7fc2b0a`)
- Security hardening and long-term data performance fixes (`1f69c89`)
- README: update scripts list (deploy.sh → deploy_rocky8.sh) (`dabc77c`)
- Add Rocky Linux 8 deploy script with remote WAS update via SSH (`1ef9a3d`)
- Update README for Rocky Linux 8 (Docker-free deployment) (`96227b0`)
- Add Rocky Linux 8 daemon install script (`8f642f0`)
- Add Rocky Linux 8 update script (Docker-free) (`1d12775`)
- Add Rocky Linux 8 install script (Docker-free, systemd-based) (`0e4a5cf`)

## 2026-06-09
- Reduce WAS load: write buffer + heartbeat dirty tracking (`517417d`)
- Add daemon status reporting to WAS (heartbeat extended) (`04aca3e`)
- Daemon: recommended config, auto-restart on abnormal exit, buffer limit (`06e35c7`)
- Daemon: 3-retry limit, duplicate prevention via position file, safe shutdown (`980910a`)
- Auto-generate all credentials on install (DB_PASSWORD, SECRET_KEY, ADMIN_PASSWORD) (`8fae2c2`)
- Optimize DB indexes: remove low-cardinality, add GIN trigram for username ILIKE (`69051e0`)
- Add README.md (`2fda451`)
- deploy.sh: continue on push failure, warn instead of exit (`5e2d723`)
- Add update.sh for EC2 WAS update (pull + rebuild + restart) (`f1a7c5e`)
- Simplify deploy.sh to git push only (GitLab + GitHub) (`e826f61`)
- Add deploy.sh and dual remote (GitLab + GitHub) (`4b268c8`)
- Initial commit: SolTrace FTP log analysis system (`f2cb34d`)
